import React, {View} from 'react-native';
import {shallow} from 'enzyme';
import MyComponent from '../Separator.js';
import {expect} from 'chai';

describe('<Separator />', () => {
  it('should render a line', () => {
    const component = shallow()
  });
});
